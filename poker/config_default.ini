[main]
terminalmode = 0
setupmode = 0
montecarlo_timeout = 8
last_strategy = Trial 1
control = Direct mouse control
table_scraper_name = Official Poker Stars
login = guest
password = guest
db = https://dickreuter.com:7778/
[flybrain]
# shadow: the fly decides and everything is recorded, the rule-based Decision still
#         drives the mouse. Its decisions are logged against real scraped states, but the
#         hand outcomes belong to the baseline - shadow rows cannot give the fly a win
#         rate. Start here to verify perception and see how often the two agree.
# active: the fly drives. Outcomes become attributable to it, so this is the only mode
#         that produces a genuine fly track record. Requires confirmed_play_money below.
# offline: no scraper; used by cli.py.
mode = shadow
scope = mushroom_body
weight_threshold = 3
plasticity = true
learning_rate = 0.10
play_money_only = true
confirmed_play_money = false
